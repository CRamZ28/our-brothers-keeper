Please provide ChatGPT (GPT-5) with detailed information about the current Our Brother’s Keeper React + Tailwind app, focusing on everything relevant to integrating a new landing page component.
Include the following details:

Routing Setup

What router is the app using (React Router, Next.js pages, HashRouter, etc.)?

Where is the main route configuration file located (e.g., App.jsx, main.jsx, router.jsx)?

What is the current entry route (home/dashboard), and where would a new route like /landing or / fit best?

Layout Structure

Are there global layout components (like Layout, Header, or Sidebar) automatically wrapping all routes?

Does the app use a layout hierarchy (e.g., DashboardLayout, PublicLayout)?

Where should a standalone landing page live — outside that layout or inside a variant of it?

Tailwind Configuration

What’s in tailwind.config.js (theme colors, fonts, plugins)?

Are custom colors or gradients already defined (like teal, mauve, or silver)?

Are there global utility classes or custom CSS layers (e.g., .glass, .btn-primary) that should be reused for visual consistency?

File & Component Structure

What’s the file structure inside src/ (especially components/, pages/, layouts/, and assets/)?

Where do public/static images and icons live (for hero background, logos, etc.)?

What’s the preferred naming convention for new files (PascalCase vs kebab-case)?

Design System & Libraries

Are any UI frameworks or component libraries being used (e.g., shadcn/ui, Headless UI, DaisyUI, Flowbite, etc.)?

Are there existing reusable components (e.g., Button, Card, Section, Container) I should use for the landing page layout?

Global State / Auth Context

How is authentication handled (Firebase Auth, Supabase, Clerk, custom)?

Should the landing page display different UI states depending on whether a user is logged in?

Is there a global context provider I should be aware of when creating a standalone public page?

Deployment Context

What deployment environment is used (Replit web server, Vercel, Netlify)?

Are environment variables or meta tags (title, description, favicon) handled manually or via framework config?

Integration Goal

Confirm the best way to integrate the landing page:

As a new / route that replaces the dashboard when logged out, or

As a standalone /landing route.

Should it reuse the app’s layout or render independently with its own background and nav bar?